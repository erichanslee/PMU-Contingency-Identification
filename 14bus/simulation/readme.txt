This folder is where the time-domain simulation data will be stored

Each "vector" of simulation data, one corresponding to each bus that
a pmu is on, is stored separately in a .txt file for FDM to process. 

This might change in the future. 
